@layout('_layout.main')

@section('content')
        
    <main>
        <section class="features section text-center section-first">
            <div class="container">
                <div class="features-inner section-inner">
                    <div class="features-wrap">
						@each(item in article)
						<div class="feature is-revealing">
								<div class="feature-inner content-card"  onclick="read({{item.id_a}},'article')">
									<div class="feature-icon">
										<img class="photo-profile" src="{{assetsUrl('images/'+item.img)}}" alt="">
									</div>
									<h4 class="feature-title h3-mobile">{{item.id_a}}</h4>
									<p class="text-sm">{{item.article_a}}</p>
								</div>
							</div>
						@endeach
                    </div>
                </div>
            </div>
        </section>
    </main>

@endsection